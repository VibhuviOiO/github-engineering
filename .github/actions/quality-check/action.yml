name: Quality Check
description: Placeholder quality check step.

inputs:
  message:
    description: Message shown in the workflow logs.
    required: false
    default: Quality check placeholder completed.

runs:
  using: composite
  steps:
    - name: Quality check placeholder
      shell: bash
      run: echo "${{ inputs.message }}"